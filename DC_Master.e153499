{Error using FilterLFP (line 94)
Index exceeds matrix dimensions.

Error in MS_amp_xcorr_session_2 (line 149)
                FILT_1 = FilterLFP(cfg_filter,
                data.(PARAMS.Phases{iPhase}).([S{1} '_pot']));

Error in Master_Multisite_postprocess (line 36)
        Naris.(PARAMS.Subjects{iSub}).(strrep(sess_list{iSess}, '-', '_')).amp
        = MS_amp_xcorr_session_2([],
        data.(PARAMS.Subjects{iSub}).(strrep(sess_list{iSess}, '-', '_')));

Error in MS_initialize_discovery (line 43)
Master_Multisite_postprocess
} 
